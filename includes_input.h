// fixes for includes
typedef float _Float16;
typedef float __bf16;

// SDL (with Image) includes (for 2D graphics, and 3D too optionally)
#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>

// OpenGL includes (for optional 3D graphics)
// #include <GL/gl.h>
// #include <GL/glu.h>
